version: 2.1
jobs:
  
  save_hello_world_output:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: exit 1
      - run: 
          name: on error
          command: echo "Hello Error!"
          when: on_fail
      - persist_to_workspace:
          root: ~/
          paths:
            - output.txt


workflows:
  my_workflow:
    jobs:
      - save_hello_world_output